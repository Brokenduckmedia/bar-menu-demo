function orderDrink(drink, table) {
    alert(`Order placed for ${drink} from ${table}`);
    // This is a placeholder. You can add code here to send the order to a backend service or Google Sheets.
}
